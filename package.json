{
  "name": "soksak-plugin-git-core",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "node --test",
    "test-unit": "node build.mjs && node --test"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  },
  "version": "0.0.1",
  "soksakRelease": {
    "kind": "plugin",
    "id": "soksak-plugin-git-core",
    "repository": "https://github.com/soksak-ai/soksak-plugin-git-core",
    "manifest": "release.json"
  }
}
